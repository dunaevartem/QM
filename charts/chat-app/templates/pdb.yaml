apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Release.Name }}-pdb
spec:
  minAvailable: 1 # Минимум один под всегда должен работать
  selector:
    matchLabels:
      app: {{ .Release.Name }}